# minihost Code Review

Scope: C API (`projects/libminihost/`), audio I/O layer (`projects/libminihost_audio/`),
nanobind bindings (`src/minihost/_core.cpp`), pure-Python layer (`src/minihost/*.py`),
and tests. Findings cite `path:line` and describe a concrete defect or improvement.

## Status update

All 14 items in Section 9 ("Recommended priorities") were addressed across
follow-up work. See **Section 10** at the end of this document for per-item
status. The findings below are preserved as written so the rationale and
code citations remain available; consult Section 10 for the resolution
of any specific item.

## 9. Recommended priorities

Ranked by impact-to-effort, highest first:

1. Fix audio-thread heap allocations in `mh_process_double`,
   `mh_chain_process_*`, and the listener trampoline (Section 2).
5. Add concurrency, channel-mismatch, and RT-allocation tests (Section 7).
6. Introduce ABI versioning macro + struct version fields before any new
   API is shipped (Section 5).
7. Tighten `mh_set_sample_rate` and `mh_set_processing_precision` to fail
   loudly when the plugin rejects the new layout/rate post-prepare
   (Sections 2 and 3).
8. Document the realtime-vs-other-thread contract more precisely in
   `minihost.h:4-9` and add `mh_close` / AudioDevice ordering rules
   (Section 3).
9. Pre-roll / trim by `latency_samples` in the Python `render_midi` path
   (Section 4).
10. Deduplicate the two `find_param_by_name` implementations and the two
    vstpreset parsers (Sections 5 and 3).
11. Stream `render_midi_to_file` instead of materializing the full
    waveform in memory (Section 6).
12. Add a `with` context manager to `Plugin` and a real `close()` method
    (Section 5).
13. Pin JUCE to a SHA in `download_juce.py`; add CMake install rules
    (Section 8).
14. Replace `desc.isInstrument` heuristic in `mh_probe` or document its
    limits (Sections 2 and 4).

---

## 10. Resolution status (post-review work)

Verified by re-reading the cited file paths and running the full test suite
(485 passed, 2 skipped). 8 new test files added, exercising the regression
surface for each fix.

| # | Item | Status | Evidence |
|---|------|--------|----------|
| 1 | Audio-thread heap allocations in `mh_process_double`, `mh_chain_process_*`, listener trampoline | DONE | `minihost.cpp:1421-1497` (process_double via persistent `processBufD`); `minihost_chain.cpp:387-411` (persistent `autoChunkIn/Out/MidiIn/MidiOut`); `_core.cpp:885-910` (listener: bounded queue + reserve + clear). Tests: `test_rt_allocations.py`, `test_concurrency.py` |
| 2 | `inCh != outCh` data-loss in `mh_process_midi_io` | DONE | `minihost.cpp:339-385` and chunk path 681-733 -- single `processBuf` sized `max(inCh+sidechainCh, outCh)`. Test: `test_asymmetric_channels.py` |
| 3 | `PluginChain` / `AudioDevice` raw-pointer GC bug | DONE | `_core.cpp:2120,2196,2219` -- `nb::keep_alive<1, 2>()` on all three constructors. Test: `test_chain_gc.py` |
| 4 | Channel-count validation on Python `process*` methods | DONE | `_core.cpp:67-94` (helper); applied in 8 process methods. Test: `test_channel_validation.py` |
| 5 | Concurrency, channel-mismatch, and RT-allocation tests | DONE | `test_concurrency.py` (set_param vs. process, callback queue ordering and overflow); `test_channel_validation.py`; `test_rt_allocations.py` |
| 6 | ABI versioning macro + struct version fields | DONE (partial -- macros + functions only; struct_size fields deferred per Section 9 design notes) | `minihost.h:80-100` (macros + decls); `minihost.cpp:32-42` (impls). Per-struct `size_t struct_size` deferred until first additive change actually needs it -- adding it now would be churn without benefit. Test: `test_api_version.py` |
| 7 | `mh_set_sample_rate` / `mh_set_processing_precision` fail loudly on rejection | DONE | `minihost.cpp:1276-1289` (sample rate post-prepare verification + NaN reject); 1380-1392 (precision post-prepare verification). Test: `test_setters_fail_loud.py` |
| 8 | Tighten realtime-vs-other-thread contract docs in `minihost.h:4-9` | DONE | `minihost.h:4-50` -- now distinguishes three classes (audio-only, concurrent-with-audio, NOT-safe-to-overlap) explicitly with full function lists, plus AudioDevice / `mh_close` ordering rules |
| 9 | Pre-roll / latency-trim in Python `render_midi` | DONE | `render.py:441-458` (init compensation), 510-616 (render_block trims first `latency_samples` of output, extends render bound by latency, uses corrected MIDI-end for auto-tail). Test: `test_render_latency_compensation.py` |
| 10 | Deduplicate `find_param_by_name` and `.vstpreset` parsers | DONE | `automation.py:28-49` -- now delegates to `Plugin.find_param`; `vstpreset.py` shrunk from 303 to 140 lines, delegating to new `_core.vstpreset_read` / `vstpreset_write` (`_core.cpp:1761-1818`) |
| 11 | Stream `render_midi_to_file` instead of materializing | DONE | `render.py:307-353` -- pre-allocates one output array sized to `MidiRenderer.total_samples` and writes blocks into slices; peak memory drops from ~3x to ~1x of final audio |
| 12 | `with` context manager + `close()` on `Plugin` | DONE | `_core.cpp:111-131` (Plugin), 939-951 (PluginChain) -- explicit `close()`, `__enter__`, `__exit__`. Test: `test_context_manager.py` |
| 13 | Pin JUCE to a SHA in `download_juce.py`; add CMake install rules | DONE (partial -- install rules without `find_package` export; see caveat) | `scripts/download_juce.py:18-43` (SHA pin with `JUCE_ALLOW_TAG=1` escape hatch); `CMakeLists.txt:25-65` and `projects/libminihost/CMakeLists.txt:39-51` (install rules gated on `MINIHOST_INSTALL`). No `find_package(minihost)` config target: the static lib has PRIVATE link deps on JUCE that this project vendors via `add_subdirectory` of a downloaded source tree, so a clean export is not achievable without first solving the JUCE-vendoring story (out of scope for this round). External consumers get `lib/libminihost.a` + `include/minihost/*.h`. |
| 14 | `mh_probe` `accepts_midi = isInstrument` heuristic | DONE (documentation-only, code unchanged) | This JUCE version's `PluginDescription` has no `wantsMidiInput` / `producesMidiOutput` fields; a real fix would require parsing VST3 `moduleinfo.json` ourselves. `minihost.h:30-31` and `minihost.cpp:847-857` now document the limitation explicitly and direct callers to `mh_get_info` for authoritative values. |

### Caveats and deferred work

- **#1 (listener trampoline)**: kept the mutex; replaced `swap()` with
  `clear()` + reserved capacity to eliminate the malloc path. Mutex hold is
  microseconds against pre-reserved capacity. A full lock-free MPSC ring
  buffer is a localized later change if measurements ever show audible
  glitches from the residual mutex.
- **#6 (ABI versioning)**: macros + `mh_api_version()` shipped at 1.0.0;
  per-struct `struct_size` fields not added (would be churn without benefit
  until the first additive change actually needs them).
- **#13 (install rules)**: ships static lib + headers; no `find_package`
  export. JUCE-as-private-dep is the blocker; documented in the install
  block.
- **#14 (`mh_probe` MIDI flags)**: documentation-only fix; the code path
  still uses `isInstrument`. Properly fixing it requires parsing VST3
  `moduleinfo.json` ourselves -- new C code that wasn't in scope.

### Test suite delta

| File | Tests | Coverage |
|------|-------|----------|
| `test_chain_gc.py` | 3 | `PluginChain` / `AudioDevice` lifetime pinning |
| `test_channel_validation.py` | 8 (6 active, 2 skip on mono) | Shape validation on all process entry points |
| `test_asymmetric_channels.py` | 4 | Combined-buffer correctness, varying block sizes |
| `test_rt_allocations.py` | 5 | Repeated-call stability for `process_double` and chain `process_auto` |
| `test_context_manager.py` | 7 | `Plugin` / `PluginChain` close + `with` semantics |
| `test_api_version.py` | 4 | Header / runtime ABI-version agreement |
| `test_concurrency.py` | 4 | `set_param` racing `process`; callback queue ordering and overflow |
| `test_setters_fail_loud.py` | 4 | `set_sample_rate` rejects negative/zero/NaN; `set_processing_precision` rejects unsupported double |
| `test_render_latency_compensation.py` | 5 | Mock-plugin verification of latency-comp skip semantics |

Total: 44 new tests across 9 files. Full suite: 485 passed, 2 skipped.
